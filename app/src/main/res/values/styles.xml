<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="roundedImageViewRounded">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <style name="NavigationViewStyle" parent="Widget.Design.NavigationView">
        <!-- Establecer el color del texto del título del NavigationView -->
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Otros estilos -->

        <!-- Agrega un color de flecha personalizado -->
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item> <!-- Color de la flecha -->
    </style>

    <style name="myCustomToolbarTheme">
        <item name="colorControlNormal">#FFFFFF</item>
    </style>

</resources>